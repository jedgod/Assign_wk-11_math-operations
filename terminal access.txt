#Stage all files
git add .

# Commit
git commit -m "Week 11: Full math operations app with add/sub/mul/polynomial + pytest + 3-stage CI/CD"

# Push to BOTH (GitLab + GitHub)
git push origin main && git push github main


#GITLAB Configuration
cd existing_repo
git remote add origin https://gitlab.com/ipmcbit/assign_wk-11_math-operations.git
git branch -M main
git push -uf origin main

#GITHUB Configuration
git remote add origin https://github.com/jedgod/Assign_wk-11_math-operations.git
git branch -M main
git push -u origin main
